# Dependency file

library(finch)
library(raster)
library(dplyr)
library(CoordinateCleaner)
library(ggplot2)
library(GGally)




weather_from_closest_station <- function(coords,
                                         error_radius = 100,
                                         years = 2016:2019) {
  
  
}